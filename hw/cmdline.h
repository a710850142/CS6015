#ifndef CMDLINE_H        // If the macro CMDLINE_H is not defined yet, proceed with the following code.
#define CMDLINE_H        // Define the macro CMDLINE_H to prevent this header file from being included multiple times.

// Declare the use_arguments function, which takes an integer argc and an array of character pointers argv as parameters.
void use_arguments(int argc, char* argv[]);

#endif                   // End of preprocessor directive, matching the start of #ifndef.
